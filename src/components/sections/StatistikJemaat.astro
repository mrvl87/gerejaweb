---
import { getSiteSettingsContent, getStatistikContent } from '../../lib/site-content';

const statistik = getStatistikContent();
const siteSettings = getSiteSettingsContent();
---

<section class="content-section surface-section">
  <div class="shell reveal">
    <div class="section-heading">
      <p class="eyebrow">{siteSettings.publicText.statistikEyebrow}</p>
      <h2>{siteSettings.publicText.statistikTitle}</h2>
    </div>

    <div class="stats-grid">
      {
        statistik.map((item) => (
          <article class="stat-card">
            <p>{item.label}</p>
            <strong>{item.angka}</strong>
            <span>{item.keterangan}</span>
          </article>
        ))
      }
    </div>
  </div>
</section>
